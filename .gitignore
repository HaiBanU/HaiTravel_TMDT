# Thư mục chứa các gói cài đặt (rất nặng và không cần đưa lên)
/node_modules
/backend/node_modules

# Tệp chứa các biến môi trường và thông tin bí mật
.env
backend/.env

# Các tệp log và debug tự động sinh ra
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Thư mục build (nếu có)
dist
build

# Tệp hệ thống của macOS
.DS_Store